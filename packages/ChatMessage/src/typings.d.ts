// Type definitions here should only be for modules that don't have their own types.
// Since all modules referenced in typings.d.ts have actual files, we remove the ambient declarations
// to avoid shadowing the real types.
